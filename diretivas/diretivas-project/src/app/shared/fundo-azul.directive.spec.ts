import { FundoAzulDirective } from './fundo-azul.directive';

describe('FundoAzulDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoAzulDirective();
    expect(directive).toBeTruthy();
  });
});
